I designed the framework to my desire, I have tried to create a simple, lightweight and an elegant object oriented design.
TODO:
    - Create view helpers (paginate)
    - Model be associated with database table? Make it optional?
    - define prefix in index page and include it in all sql queries, even if its empty
	- Make form validation more elegant. 
    - Remove logging